import styles from './Footer.module.scss';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span className={styles.title}>Здесь должен быть футер...</span>
    </footer>
  );
}

export default Footer;